<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.sdkj.meter.mapper.MtCentratorArchiveMapper">

    <delete id="deleteMeterMatch">
        DELETE FROM mt_meter_match WHERE archive_id = #{archiveId}
    </delete>

    <insert id="insertMeterToAgent">
        INSERT INTO mt_meter_match (id, archive_id, meter_type, create_by, create_time)
        VALUES (${@com.baomidou.mybatisplus.core.toolkit.IdWorker@getId()}, #{entity.id}, '11', #{entity.createBy}, NOW())
    </insert>

</mapper>
