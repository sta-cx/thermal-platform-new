<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.sdkj.thermal.mapper.PrExpenseMapper">

    <select id="selectPageList" resultType="org.sdkj.thermal.domain.vo.PrExpenseVo">
        SELECT a.id, a.house_id, a.item_group, a.item_code, a.item_name, a.standard_id,
               a.start_date, a.expire_date, a.last_date, a.last_reading, a.this_reading,
               a.qty, a.money, a.standard_price, a.max_price, a.price_formula, a.trade_times,
               a.max_money, a.money_formula, a.is_free, a.reason, a.preferential, a.deduction,
               a.latefee, a.receivable, a.paid_in, a.final_money, a.overdue_day,
               a.is_charged, a.charged_time, a.record_id, a.delay_date, a.heat_usage,
               a.is_calc, a.is_closed, a.year, a.month, a.org_id,
               a.parking_space_id,
               b.room_num,
               c.user_name,
               d.name AS building_name,
               e.parking_code,
               e.parkinglot_name
        FROM pr_expense a
        LEFT JOIN pr_house b ON a.house_id = b.id
        LEFT JOIN pr_user_house c ON c.house_id = b.id AND c.del_flag = '0'
        LEFT JOIN pr_building d ON b.building_id = d.id
        LEFT JOIN pm_parking_space e ON a.parking_space_id = e.id
        WHERE a.del_flag = '0'
                <if test="orgId != null and orgId != ''">
            AND a.org_id = #{orgId}
        </if>
        <if test="buildingId != null and buildingId != ''">
            AND b.building_id = #{buildingId}
        </if>
        <if test="unitCode != null and unitCode != ''">
            AND b.unit_code = #{unitCode}
        </if>
        <if test="itemGroup != null and itemGroup != ''">
            AND a.item_group = #{itemGroup}
        </if>
        <if test="itemCode != null and itemCode != ''">
            AND a.item_code = #{itemCode}
        </if>
        <if test="isCharged != null and isCharged != ''">
            AND a.is_charged = #{isCharged}
        </if>
        <if test="parkingId != null and parkingId != ''">
            AND a.parking_space_id = #{parkingId}
        </if>
        <if test="search != null and search != ''">
            AND (b.room_num LIKE CONCAT('%', #{search}, '%') OR c.user_name LIKE CONCAT('%', #{search}, '%'))
        </if>
        ORDER BY a.create_time DESC
    </select>

    <select id="selectHouseExpenseList" resultType="org.sdkj.thermal.domain.vo.PrExpenseVo">
        SELECT a.id, a.house_id, a.item_group, a.item_code, a.item_name, a.standard_id,
               a.start_date, a.expire_date, a.money, a.standard_price, a.is_charged,
               b.room_num, c.user_name
        FROM pr_expense a
        LEFT JOIN pr_house b ON a.house_id = b.id
        LEFT JOIN pr_user_house c ON c.house_id = b.id AND c.del_flag = '0'
        WHERE a.del_flag = '0'
          AND a.org_id = #{orgId}
        <if test="buildingId != null and buildingId != ''">
            AND b.building_id = #{buildingId}
        </if>
        <if test="unitCode != null and unitCode != ''">
            AND b.unit_code = #{unitCode}
        </if>
        <if test="itemGroup != null and itemGroup != ''">
            AND a.item_group = #{itemGroup}
        </if>
        <if test="itemCode != null and itemCode != ''">
            AND a.item_code = #{itemCode}
        </if>
        <if test="search != null and search != ''">
            AND (b.room_num LIKE CONCAT('%', #{search}, '%') OR c.user_name LIKE CONCAT('%', #{search}, '%'))
        </if>
        ORDER BY a.create_time DESC
    </select>

    <select id="selectHeatExpenseByHouseId" resultType="org.sdkj.thermal.domain.vo.PrExpenseVo">
        SELECT id, house_id, item_group, item_code, item_name, standard_id,
               start_date, expire_date, last_date, money, standard_price, is_charged
        FROM pr_expense
        WHERE house_id = #{houseId}
          AND (item_group IN ('HEAT', '6') OR item_code IN ('HEAT_FEE', '601'))
          AND del_flag = '0'
        ORDER BY is_charged DESC, start_date DESC, create_time DESC
        LIMIT 1
    </select>

    <select id="selectExistingExpenseHouseIds" resultType="java.lang.String">
        SELECT DISTINCT house_id FROM pr_expense
        WHERE item_group = #{itemGroup} AND item_code = #{itemCode} AND del_flag = '0'
    </select>

    <update id="updateStepPrice">
        UPDATE pr_expense a
        LEFT JOIN pr_standard_price b ON b.standard_id = a.standard_id
        LEFT JOIN pr_house c ON c.id = a.house_id
        SET a.standard_price = b.standard_price,
            a.price_formula = b.price_formula,
            a.max_price = b.max_price
        WHERE a.org_id = #{orgId}
          AND a.is_calc = '0'
          AND b.grade = '1'
          AND a.del_flag = '0'
    </update>

    <update id="setStandardPriceJzmj">
        UPDATE pr_expense a
        LEFT JOIN pr_standard_price b ON b.standard_id = a.standard_id
        LEFT JOIN pr_house c ON c.id = a.house_id
        SET a.standard_price = b.standard_price,
            a.price_formula = b.price_formula,
            a.max_price = b.max_price
        WHERE a.standard_id = #{standardId}
          AND a.is_calc = '0'
          AND b.minimum &lt;= c.gfloor_area
          AND b.maximum &gt;= c.gfloor_area
          AND a.del_flag = '0'
    </update>

    <update id="setStandardPriceSymj">
        UPDATE pr_expense a
        LEFT JOIN pr_standard_price b ON b.standard_id = a.standard_id
        LEFT JOIN pr_house c ON c.id = a.house_id
        SET a.standard_price = b.standard_price,
            a.price_formula = b.price_formula,
            a.max_price = b.max_price
        WHERE a.standard_id = #{standardId}
          AND a.is_calc = '0'
          AND b.minimum &lt;= c.nfloor_area
          AND b.maximum &gt;= c.nfloor_area
          AND a.del_flag = '0'
    </update>

    <update id="setStandardPriceLc">
        UPDATE pr_expense a
        LEFT JOIN pr_standard_price b ON b.standard_id = a.standard_id
        LEFT JOIN pr_house c ON c.id = a.house_id
        SET a.standard_price = b.standard_price,
            a.price_formula = b.price_formula,
            a.max_price = b.max_price
        WHERE a.standard_id = #{standardId}
          AND a.is_calc = '0'
          AND b.minimum &lt;= c.floor
          AND b.maximum &gt;= c.floor
          AND a.del_flag = '0'
    </update>

    <select id="selectDistinctPriceFormula" resultType="java.lang.String">
        SELECT DISTINCT price_formula FROM pr_expense
        WHERE org_id = #{orgId}
          AND is_calc = '0'
          AND del_flag = '0'
          AND price_formula IS NOT NULL AND price_formula != ''
          AND standard_id IS NOT NULL AND standard_id != ''
    </select>

    <select id="selectDistinctMoneyFormula" resultType="java.lang.String">
        SELECT DISTINCT money_formula FROM pr_expense
        WHERE org_id = #{orgId}
          AND is_calc = '0'
          AND del_flag = '0'
          AND money_formula IS NOT NULL AND money_formula != ''
          AND standard_id IS NOT NULL AND standard_id != ''
    </select>

    <!-- 公式来自数据库列值，通过 ${} 做字符串替换拼入 SQL（老系统设计） -->
    <update id="updateMoney">
        UPDATE pr_expense
            LEFT JOIN pr_house ON pr_house.id = pr_expense.house_id
            LEFT JOIN pr_standard ON pr_standard.id = pr_expense.standard_id
            LEFT JOIN pr_standard_price ON pr_standard_price.standard_id = pr_expense.standard_id
            LEFT JOIN pr_expense_item ON pr_expense_item.item_code = pr_standard.item_code
                AND pr_expense_item.item_group = pr_standard.item_group
                AND pr_expense_item.org_id = pr_standard.org_id
        SET pr_expense.standard_price = IF(pr_expense.max_price = 0,
            ROUND(${priceFormula}, pr_expense_item.price_precision),
            IF(ROUND(${priceFormula}, pr_expense_item.price_precision) >= pr_expense.max_price,
                pr_expense.max_price,
                ROUND(${priceFormula}, pr_expense_item.price_precision)))
        WHERE pr_expense.org_id = #{orgId}
          AND pr_expense.is_calc = '0'
          AND pr_expense.price_formula = #{priceFormula}
          AND pr_expense.del_flag = '0'
    </update>

    <!-- 公式来自数据库列值，通过 ${} 做字符串替换拼入 SQL（老系统设计） -->
    <update id="updateFormula">
        UPDATE pr_expense
            LEFT JOIN pr_house ON pr_house.id = pr_expense.house_id
            LEFT JOIN pr_standard ON pr_standard.id = pr_expense.standard_id
            LEFT JOIN pr_standard_price ON pr_standard_price.standard_id = pr_expense.standard_id
            LEFT JOIN pr_expense_item ON pr_expense_item.item_group = pr_standard.item_group
                AND pr_expense_item.item_code = pr_standard.item_code
                AND pr_expense_item.org_id = pr_standard.org_id
        SET pr_expense.is_calc = '1',
            pr_expense.receivable = IF(pr_expense.max_money > '0' AND ROUND(${moneyFormula}, pr_expense_item.money_precision) >= pr_expense.max_money,
                pr_expense.max_money,
                ROUND(${moneyFormula}, pr_expense_item.money_precision)),
            pr_expense.final_money = ROUND(
                IF(pr_expense.max_money > '0' AND ROUND(${moneyFormula}, pr_expense_item.money_precision) >= pr_expense.max_money,
                    pr_expense.max_money,
                    ROUND(${moneyFormula}, pr_expense_item.money_precision))
                - pr_expense.preferential - pr_expense.deduction + pr_expense.latefee,
                pr_expense_item.money_precision)
        WHERE pr_expense.org_id = #{orgId}
          AND pr_expense.is_calc = '0'
          AND pr_expense.money_formula = #{moneyFormula}
          AND pr_expense.del_flag = '0'
    </update>

    <!-- 公式来自数据库列值，通过 ${} 做字符串替换拼入 SQL（老系统设计） -->
    <update id="updateFormulaCw">
        UPDATE pr_expense
            LEFT JOIN pr_house ON pr_house.id = pr_expense.house_id
            LEFT JOIN pm_standard ON pm_standard.id = pr_expense.standard_id
        SET pr_expense.is_calc = '1',
            pr_expense.receivable = IF(pr_expense.max_money > '0' AND ROUND(${moneyFormula}, '2') >= pr_expense.max_money,
                pr_expense.max_money,
                ROUND(${moneyFormula}, '2')),
            pr_expense.final_money = ROUND(
                IF(pr_expense.max_money > '0' AND ROUND(${moneyFormula}, '2') >= pr_expense.max_money,
                    pr_expense.max_money,
                    ROUND(${moneyFormula}, '2'))
            - pr_expense.preferential - pr_expense.deduction + pr_expense.latefee, '2')
        WHERE pr_expense.org_id = #{orgId}
          AND pr_expense.is_calc = '0'
          AND pr_expense.money_formula = #{moneyFormula}
          AND pr_expense.del_flag = '0'
    </update>

    <update id="updateHousePayStatus">
        UPDATE pr_house SET is_charged = 1
        WHERE id IN
        <foreach collection="houseIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="updateCalStatus">
        UPDATE pr_house SET is_calc = '1'
        WHERE id = #{houseId}
    </update>

    <select id="selectStandardById" resultType="org.sdkj.thermal.domain.PrStandard">
        SELECT id, name, item_code, item_group, cycles, generate_rule, step_type,
               statistics_type, step_maxgrade, is_step2, step2_type, step2_maxgrade,
               is_latefee, latefee_startdate, latefee_type, latefee_startdays, latefee_formula,
               is_limited, limited_type, limited_cond, limited_times, limited_money,
               limited_single_money, standard_price, money_formula, max_money,
               org_id
        FROM pr_standard
        WHERE id = #{standardId}
    </select>

    <select id="selectParkingPageList" resultType="org.sdkj.thermal.domain.vo.PrExpenseVo">
        SELECT a.id, a.house_id, a.item_group, a.item_code, a.item_name, a.standard_id,
               a.start_date, a.expire_date, a.money, a.standard_price, a.is_free,
               a.preferential, a.deduction, a.receivable, a.paid_in, a.final_money,
               a.is_charged, a.charged_time, a.parking_space_id, a.qty, a.year, a.month,
               b.room_num, c.user_name, d.name AS building_name,
               e.parking_code, e.parkinglot_name
        FROM pr_expense a
        LEFT JOIN pr_house b ON a.house_id = b.id
        LEFT JOIN pr_user_house uh ON uh.house_id = b.id AND uh.del_flag = '0'
        LEFT JOIN pr_user c ON uh.user_id = c.id
        LEFT JOIN pr_building d ON b.building_id = d.id
        LEFT JOIN pm_parking_space e ON a.parking_space_id = e.id
        WHERE a.del_flag = '0'
          AND a.org_id = #{orgId}
        <if test="buildingId != null and buildingId != ''">AND b.building_id = #{buildingId}</if>
        <if test="unitCode != null and unitCode != ''">AND b.unit_code = #{unitCode}</if>
        <if test="itemGroup != null and itemGroup != ''">AND a.item_group = #{itemGroup}</if>
        <if test="itemCode != null and itemCode != ''">AND a.item_code = #{itemCode}</if>
        <if test="isCharged != null and isCharged != ''">AND a.is_charged = #{isCharged}</if>
        <if test="parkingId != null and parkingId != ''">AND e.parking_id = #{parkingId}</if>
        <if test="search != null and search != ''">AND (b.room_num LIKE CONCAT('%', #{search}, '%') OR e.parking_code LIKE CONCAT('%', #{search}, '%'))</if>
        ORDER BY a.create_time DESC
    </select>

    <select id="selectParkingSpaceExpenseList" resultType="java.util.LinkedHashMap">
        SELECT a.id, a.code AS parkingCode, a.house_id,
               e.user_name AS userName, '7' AS itemGroup, '701' AS itemCode,
               a.standard_id, a.org_id,
               b.room_num AS roomNum,
               IFNULL(g.expire_date, a.establish_time) AS openTime,
               c.name AS standardName, c.standard_price AS standardPrice
        FROM pm_parking_space a
        LEFT JOIN pr_house b ON b.id = a.house_id
        LEFT JOIN pm_standard c ON c.id = a.standard_id
        LEFT JOIN pr_user_house uh ON uh.house_id = a.house_id AND uh.del_flag = '0'
        LEFT JOIN pr_user e ON uh.user_id = e.id
        LEFT JOIN (
            SELECT parking_space_id, MAX(expire_date) AS expire_date
            FROM pr_expense WHERE del_flag = '0' GROUP BY parking_space_id
        ) g ON g.parking_space_id = a.id
        WHERE a.org_id = #{orgId}
        <if test="buildingId != null and buildingId != ''">AND b.building_id = #{buildingId}</if>
        <if test="unitCode != null and unitCode != ''">AND b.unit_code = #{unitCode}</if>
        <if test="itemGroup != null and itemGroup != ''">AND '7' = #{itemGroup}</if>
        <if test="parkingId != null and parkingId != ''">AND a.parking_id = #{parkingId}</if>
        <if test="search != null and search != ''">AND (b.room_num LIKE CONCAT('%', #{search}, '%') OR a.code LIKE CONCAT('%', #{search}, '%'))</if>
    </select>

    <select id="selectHouseExpenseAllList" resultType="java.util.LinkedHashMap">
        SELECT a.id, a.house_id, a.item_group, a.item_code, a.standard_id,
               b.building_id AS buildingId, b.unit_code AS unitCode
        FROM pr_house_expense a
        LEFT JOIN pr_house b ON b.id = a.house_id
        WHERE a.org_id = #{orgId}
        <if test="search != null and search != ''">AND b.room_num = TRIM(#{search})</if>
    </select>

    <select id="selectExpenseLog" resultType="java.util.LinkedHashMap">
        SELECT a.*, b.org_id, b.org_name, b.building_id, b.building_name, b.unit_code, b.room_num,
               b.other_code, c.user_name, c.phone, d.user_name AS create_by_name
        FROM pr_expense_log a
        LEFT JOIN pr_house b ON b.id = a.house_id
        LEFT JOIN pr_user_house uh ON uh.house_id = b.id AND uh.del_flag = '0'
        LEFT JOIN pr_user c ON uh.user_id = c.id
        LEFT JOIN sys_user d ON d.id = a.create_by
        LEFT JOIN sys_organization e ON e.id = b.org_id
        WHERE 1=1
        <if test="orgId != null and orgId != ''">AND b.org_id = #{orgId}</if>
        <if test="buildingId != null and buildingId != ''">AND b.building_id = #{buildingId}</if>
        <if test="unitCode != null and unitCode != ''">AND b.unit_code = #{unitCode}</if>
        <if test="parentId != null and parentId != ''">AND e.parent_id = #{parentId}</if>
        <if test="type != null and type != ''">AND a.log_type = #{type}</if>
        <if test="startTime != null and startTime != ''">AND a.create_time &gt;= #{startTime}</if>
        <if test="endTime != null and endTime != ''">AND a.create_time &lt;= #{endTime}</if>
        <if test="search != null and search != ''">AND (b.room_num LIKE CONCAT('%', #{search}, '%') OR b.other_code LIKE CONCAT('%', #{search}, '%') OR c.phone LIKE CONCAT('%', #{search}, '%'))</if>
        ORDER BY a.create_time DESC
    </select>

    <!-- 滞纳金计算 - 起收日期 -->
    <update id="updateLatefeeQs">
        UPDATE pr_expense a
        LEFT JOIN pr_standard b ON b.id = a.standard_id
        LEFT JOIN pr_expense_item c ON c.item_group = b.item_group AND c.item_code = b.item_code
        SET a.latefee = ROUND(
            (${latefeeFormula}) *
            IF(DATEDIFF(CURRENT_DATE, DATE_ADD(a.start_date, INTERVAL b.latefee_startdays DAY)) &lt;= 0,
                0,
                DATEDIFF(CURRENT_DATE, DATE_ADD(a.start_date, INTERVAL b.latefee_startdays DAY))),
            c.money_precision),
            a.overdue_day = IF(DATEDIFF(CURRENT_DATE, DATE_ADD(a.start_date, INTERVAL b.latefee_startdays DAY)) &lt;= 0,
                0,
                DATEDIFF(CURRENT_DATE, DATE_ADD(a.start_date, INTERVAL b.latefee_startdays DAY)))
        WHERE a.org_id = #{orgId}
          AND b.id = #{standardId}
          AND a.is_charged = '0'
          AND a.is_calc = '1'
          AND DATE_FORMAT(a.start_date, '%Y-%m') &lt; DATE_FORMAT(CURRENT_DATE, '%Y-%m')
          AND a.del_flag = '0'
    </update>

    <!-- 滞纳金计算 - 到期日期 -->
    <update id="updateLatefeeJs">
        UPDATE pr_expense a
        LEFT JOIN pr_standard b ON b.id = a.standard_id
        LEFT JOIN pr_expense_item c ON c.item_group = b.item_group AND c.item_code = b.item_code
        SET a.latefee = ROUND(
            (${latefeeFormula}) *
            IF(DATEDIFF(CURRENT_DATE, DATE_ADD(a.expire_date, INTERVAL b.latefee_startdays DAY)) &lt;= 0,
                0,
                DATEDIFF(CURRENT_DATE, DATE_ADD(a.expire_date, INTERVAL b.latefee_startdays DAY))),
            c.money_precision),
            a.overdue_day = IF(DATEDIFF(CURRENT_DATE, DATE_ADD(a.expire_date, INTERVAL b.latefee_startdays DAY)) &lt;= 0,
                0,
                DATEDIFF(CURRENT_DATE, DATE_ADD(a.expire_date, INTERVAL b.latefee_startdays DAY)))
        WHERE a.org_id = #{orgId}
          AND b.id = #{standardId}
          AND a.is_charged = '0'
          AND a.is_calc = '1'
          AND DATE_FORMAT(a.expire_date, '%Y-%m') &lt; DATE_FORMAT(CURRENT_DATE, '%Y-%m')
          AND a.del_flag = '0'
    </update>

    <!-- 滞纳金计算 - 指定日期 -->
    <update id="updateLatefeeZd">
        UPDATE pr_expense a
        LEFT JOIN pr_standard b ON b.id = a.standard_id
        LEFT JOIN pr_expense_item c ON c.item_group = b.item_group AND c.item_code = b.item_code
        SET a.latefee = ROUND(
            (${latefeeFormula}) *
            IF(DATEDIFF(CURRENT_DATE, DATE_ADD(b.latefee_startdate, INTERVAL b.latefee_startdays DAY)) &lt;= 0,
                0,
                DATEDIFF(CURRENT_DATE, DATE_ADD(b.latefee_startdate, INTERVAL b.latefee_startdays DAY))),
            c.money_precision),
            a.overdue_day = IF(DATEDIFF(CURRENT_DATE, DATE_ADD(b.latefee_startdate, INTERVAL b.latefee_startdays DAY)) &lt;= 0,
                0,
                DATEDIFF(CURRENT_DATE, DATE_ADD(b.latefee_startdate, INTERVAL b.latefee_startdays DAY)))
        WHERE a.org_id = #{orgId}
          AND b.id = #{standardId}
          AND a.is_charged = '0'
          AND a.is_calc = '1'
          AND DATE_FORMAT(b.latefee_startdate, '%Y-%m') &lt; DATE_FORMAT(CURRENT_DATE, '%Y-%m')
          AND a.del_flag = '0'
    </update>

    <!-- 滞纳金计算 - 数据核查 -->
    <update id="updateLatefeeSJHC">
        UPDATE pr_expense a
        LEFT JOIN pr_standard b ON b.id = a.standard_id
        LEFT JOIN pr_expense_item c ON c.item_group = b.item_group AND c.item_code = b.item_code
        SET a.latefee = ROUND(
            (${latefeeFormula}) *
            IF((DATEDIFF(CURRENT_DATE, DATE_ADD(a.start_date, INTERVAL b.latefee_startdays DAY)) - '30') &lt;= 0,
                0,
                (DATEDIFF(CURRENT_DATE, DATE_ADD(a.start_date, INTERVAL b.latefee_startdays DAY)) - '30')),
            c.money_precision),
            a.overdue_day = IF((DATEDIFF(CURRENT_DATE, DATE_ADD(a.start_date, INTERVAL b.latefee_startdays DAY)) - '30') &lt;= 0,
                0,
                (DATEDIFF(CURRENT_DATE, DATE_ADD(a.start_date, INTERVAL b.latefee_startdays DAY)) - '30'))
        WHERE a.org_id = #{orgId}
          AND b.id = #{standardId}
          AND a.is_charged = '0'
          AND a.is_calc = '1'
          AND a.year = #{year}
          AND a.month = #{month}
          AND a.del_flag = '0'
    </update>

    <!-- 滞纳金计算后更新最终金额 -->
    <update id="updateFinalMoneyAfterLateFee">
        UPDATE pr_expense a
        LEFT JOIN pr_standard b ON b.id = a.standard_id
        LEFT JOIN pr_expense_item c ON c.item_group = a.item_group
            AND c.item_code = a.item_code
            AND c.org_id = a.org_id
        SET a.final_money = ROUND(a.receivable - a.preferential - a.deduction + a.latefee, c.money_precision)
        WHERE a.org_id = #{orgId}
          AND a.is_charged = '0'
          AND a.is_calc = '1'
          AND b.id = #{standardId}
          AND a.del_flag = '0'
    </update>

</mapper>
