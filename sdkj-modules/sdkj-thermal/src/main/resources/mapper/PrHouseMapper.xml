<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.sdkj.thermal.mapper.PrHouseMapper">

    <select id="selectOtherCodeById" resultType="string">
        SELECT other_code FROM pr_house WHERE id = #{id}
    </select>

    <update id="updateOtherCodeById">
        UPDATE pr_house SET other_code = #{otherCode} WHERE id = #{id}
    </update>

</mapper>
